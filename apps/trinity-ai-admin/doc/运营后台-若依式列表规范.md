# 运营后台 · 若依式列表规范

> **真源实现**：`src/views/admin-keys/KeysPage.vue`（平台密钥 / 用户密钥 / 审计轨迹三表）  
> **适用范围**：运营后台所有 **「检索 + 筛选 + 表格 + 分页」** 子页（计费、供应商、模型、客户、用户、权限、系统、风控等）。  
> **非列表页**（工作台、实时大盘、文档编辑器等）不适用，见文末 §8。

与 Element Plus / 全局样式关系：

| 层级 | 路径 | 职责 |
|------|------|------|
| 组件约定 | 本文 | DOM 结构、工具栏、表格列、操作列 |
| 列宽档位 | `src/utils/adminTableColumns.ts` | `ADMIN_TABLE_COL` / `ADMIN_TABLE_COL_OPS` |
| 表头与单元格 | `src/styles/admin-ruoyi.css` | `.admin-ep-table-wrap`、`.admin-ep-row-actions` |
| 查询栏布局 | `src/styles/admin-page.css` | `.admin-section-head--toolbar` |
| App 总览 | [`README.md`](../README.md) | UI 栈、色彩 |

交付计划交叉引用：[`docs/AI-API聚合平台-运营后台-原型交付计划.md`](../../../docs/AI-API聚合平台-运营后台-原型交付计划.md) **§2.1**（工具栏齐平、筛选形式）；产品备忘 **B-01、B-05**。

---

## 1. 页面结构（固定骨架）

面包屑 / TagsView 已由壳层展示 **当前二级页名称**，卡片内 **不再重复大标题**。

```
{module}-page                          ← 模块根，如 key-page / risk-page
└─ section.{module}-page__panel       ← v-show 切换子路由分区
   └─ el-card.admin-ep-card.{module}-page__panel
      ├─ AdminSectionHead [toolbar-only]
      │  ├─ #annot   → AdminInternalTip（ⓘ 产品说明）
      │  └─ #tools   → AdminListQuery
      ├─ el-table.admin-ep-table-wrap
      ├─ AdminTablePagination
      └─ p.{module}-page__hint（可选，底栏弱说明）
```

### 1.1 禁止写法（导致「很 low」的常见原因）

| 禁止 | 应改为 |
|------|--------|
| 卡片内 `AdminSectionHead` **带 title + #desc** 与工具栏并排 | **`toolbar-only`**，说明进 ⓘ |
| 工具栏与表格之间大块 `__context` 黄条 / 重复分工文案 | 收进 `AdminInternalTip` |
| 数据列 `width="88"` / `align="right"` / `align="center"` | 仅 `min-width` + 档位常量，**全部左对齐**（§3.2.1） |
| 操作列裸放一个 `el-button`，无 `admin-ep-row-actions` | 见 §5 |
| `el-table` 加 `border` / `stripe` | 只用 `admin-ep-table-wrap`（若依横线表） |
| 多列都设很大 `min-width` 把表撑死 | 主列 1～2 个用 `xl`/`primary`，其余 `sm`～`md` |

---

## 2. 查询工具栏（AdminSectionHead + AdminListQuery）

### 2.1 AdminSectionHead

```vue
<AdminSectionHead toolbar-only>
  <template #annot>
    <AdminInternalTip heading="… · 原型" explain="…">
      <p>…</p>
    </AdminInternalTip>
  </template>
  <template #tools>
    <AdminListQuery … />
  </template>
</AdminSectionHead>
```

- **必须** `toolbar-only`：底部分割线、检索与筛选同一行、右侧主操作齐平（样式在 `admin-page.css`）。
- **不要** `#desc` 槽（副标题由面包屑承担）。
- `title` 属性在 `toolbar-only` 下可不传。

### 2.2 AdminListQuery 布局

| 区域 | 插槽 | 内容 |
|------|------|------|
| 左侧主区 | 默认 + `#filters` + 默认插槽（日期） | 搜索框 → `el-select` 筛选 → `AdminDateRangePicker`（若需要）→ **重置** |
| 右侧 | `#actions` | **新建**（`type="primary"`）、`AdminExportCsvButton` 等 |

**左右分区（必须）**：筛选、搜索、重置只在 **`admin-list-query__main`**（左）；主操作只在 **`#actions`**（右，`margin-left: auto`）。**禁止**把 `el-select` 与「编辑/新建」等按钮并列写在 `AdminSectionHead` 的 `#tools` 里而不包 `AdminListQuery`（会与 API 密钥页不一致）。

无关键词检索时（如档案页仅供应商下拉）：`AdminListQuery` 设 **`:show-search="false"`**，筛选仍放 `#filters`，操作仍放 `#actions`。

约定：

- 搜索 `placeholder` 写清可搜字段（如「指纹前缀、名称、客户…」），宽度由全局 CSS 控制（约 16rem）。
- 筛选下拉 `placeholder` 用「状态」「动作」「客户组织」等短标签；`style="width: 7rem"` 或组织类 `10rem`。
- **重置**：`@reset` 里清空本表所有筛选 ref（含日期范围），不只清搜索词。
- 主操作（新建/导入）放 `#actions`，与密钥页一致，**不要**放在 filters 里。

### 2.3 工具栏与操作列 · 按钮间距（左右一致）

> **走查结论（2026-05-19）**：右侧 `#actions`（新增 / 导入 / 导出等）不得比左侧（搜索 / 筛选 / 重置）更「挤」或更「疏」；表格**操作列**行内按钮与工具栏使用**同一间距档位**。

| 区域 | 间距 | 样式真源 |
|------|------|----------|
| 工具栏左侧 `admin-list-query__main` | **`gap: 0.5rem`** | `admin-page.css` |
| 工具栏右侧 `admin-list-query__actions` | **`gap: 0.5rem`**（与左侧相同） | `admin-page.css` |
| 表格操作列 `admin-ep-row-actions` | **`gap: 0.5rem`**（与工具栏相同） | `admin-ruoyi.css` |

**必须遵守**

1. **Flex `gap` 为主**：相邻控件只靠容器 `gap` 分隔，**禁止**再叠 Element Plus 默认的 `.el-button + .el-button { margin-left: 12px }`。
2. **插槽按钮在父组件渲染**：`#actions` 内 `el-button` 写在 `*Page.vue`，**不会**吃到 `AdminListQuery.vue` 的 scoped 样式，须在 **`admin-page.css`** 用全局选择器清零相邻 margin（已内置，新页勿删）：

```css
.admin-shell__main .admin-section-head--toolbar .admin-list-query__main > .el-button + .el-button,
.admin-shell__main .admin-section-head--toolbar .admin-list-query__actions > .el-button + .el-button {
  margin-left: 0;
}
```

3. **操作列**：多个 `link` 按钮包在 **`div.admin-ep-row-actions`** 内，`gap: 0.5rem`；各按钮 `margin-left: 0`。
4. **禁止**在 `#actions` 或操作列外包一层带 `margin-left` 的自定义容器把间距撑大。

**自测**：供应商列表等「左筛选 + 右三按钮 + 行内编辑/删除」页，目测左右、行内间距应一致；开发者工具中相邻按钮之间不应出现 `12px` 的 EP 默认 margin。

---

## 3. 表格（el-table）

### 3.1 容器属性

```vue
<el-table
  :data="pg.paginatedRows"
  row-key="id"
  class="admin-ep-table-wrap"
  style="width: 100%"
  :default-sort="{ prop: 'updatedAt', order: 'descending' }"
>
```

| 属性 | 要求 |
|------|------|
| `class` | **`admin-ep-table-wrap`**（必填） |
| `style` | `width: 100%` |
| `border` / `stripe` | **不加** |
| `table-layout: fixed` | **禁止**（全局勿覆盖） |
| `default-sort` | 建议：时间类列降序 |

### 3.1.1 自然滚动（无卡片内固定表头）

默认布局下，卡片内 `.admin-ep-table-wrap` 单独纵向滚动，表头会钉在卡片顶部。若需 **整页（主区）一起滚、表头随内容滚走**，在模块根节点加 **`{module}-page--flow`**（如 `cus-page--flow`），样式见 `admin-ruoyi.css`。

| 类名 | 效果 |
|------|------|
| `{module}-page--flow` | 主区滚动；取消卡片/表体内嵌滚动与 EP 表头钉住 |
| 窄屏 `≤960px` | 表格区域可横向滑动（`min-width` 列保持可读） |

操作列 **可不设** `fixed="right"`，避免窄屏双滚动条；宽度仍用 `ADMIN_TABLE_COL_OPS.*`。

### 3.2 数据列

```vue
<el-table-column
  prop="displayName"
  label="密钥名"
  :min-width="ADMIN_TABLE_COL.primary"
  sortable
  show-overflow-tooltip
/>
```

| 规则 | 说明 |
|------|------|
| 宽度 | 只用 **`:min-width="ADMIN_TABLE_COL.*"`** 或 `ADMIN_TABLE_COL_MIN.detail` |
| 对齐 | **不写 `align`**（表头与数据、操作列均左对齐）；见 **§3.2.1** |
| 排序 | 需要则 `sortable` |
| 长文本 | `show-overflow-tooltip` |
| 主列 | 1 列用 `xl` 或 `primary`（名称、客户/项目等） |
| 指纹 / ID / 时间 | 可用 `key-page__mono` 等模块 **等宽字体** 类，**不要**为此单独右对齐 |
| 双行主从 | 模板内 `<br />` + 次要行 `__muted`（见 Keys 客户/项目列） |

**档位速查**（`adminTableColumns.ts`）：

| 常量 | 约宽 | 典型列 |
|------|------|--------|
| `xs` | 80 | 状态、动作、绑定数 |
| `sm` | 96 | 日期、短数字 |
| `md` | 112 | 短文案、策略版本 |
| `lg` | 128 | 时间、指纹 |
| `xl` | 144 | 客户/项目、用户/Workspace |
| `primary` | 120 | 主标题列 |
| `MIN.detail` | 200 | 说明、备注 |

### 3.2.1 单元格对齐（全部左对齐）

列表表格 **表头、数据列、操作列** 均 **左对齐**（与 `KeysPage` 一致）。`el-table-column` **不写** `align="center"` / `align="right"`；样式由 `admin-ruoyi.css` 保证。

操作列换行时按钮组仍从左侧起排（`admin-ep-row-actions` + `flex-wrap`），**不要**对操作列单独居中（易导致换行后第二行错位）。

**自测**：宽列内容贴左；操作列多按钮换行时各行左缘对齐。

### 3.3 自定义单元格（状态、枚举）

```vue
<el-table-column prop="status" label="状态" :min-width="ADMIN_TABLE_COL.xs" sortable>
  <template #default="scope">
    <template v-if="scope?.row">
      <span :class="statusBadgeClass(scope.row.status)">{{ scope.row.status }}</span>
    </template>
  </template>
</el-table-column>
```

- **必须** `v-if="scope?.row"`（避免 EP 表头占位行报错，备忘 B-05）。
- 状态徽章类名建议：`{module}-page__badge` + `--ok` / `--warn` / `--danger` / `--muted`（参考 `keys.css`）。

---

## 4. 操作列（唯一可固定 width 的列）

```vue
<el-table-column label="操作" :width="ADMIN_TABLE_COL_OPS.lg" fixed="right">
  <template #default="scope">
    <template v-if="scope?.row">
      <div class="admin-ep-row-actions" @click.stop>
        <el-button link type="primary" :icon="View" @click="openDetail(scope.row)">详情</el-button>
        <el-button link type="primary" :icon="Edit" @click="openForm('edit', scope.row)">编辑</el-button>
        <!-- 危险操作用 type="danger" + 二次确认弹窗 -->
      </div>
    </template>
  </template>
</el-table-column>
```

| 规则 | 说明 |
|------|------|
| 宽度 | `ADMIN_TABLE_COL_OPS.sm`（1 个操作）/ `md`（2～3）/ **`lg`（4+ 或 详情+编辑+冻结…）** |
| 固定 | `fixed="right"` |
| 容器 | **`div.admin-ep-row-actions`** + `@click.stop` |
| 间距 | 与工具栏一致：**`gap: 0.5rem`**，见 **§2.3** |
| 按钮 | `el-button` **`link`** + **`@element-plus/icons-vue`** 图标 + 文案 |
| 危险 | `type="danger"`（删除/吊销），须 `AdminDialog` 二次确认 |
| 禁止 | 单独 `el-switch` 列、无图标纯文字漂在列中 |

---

## 5. 分页与底栏

```vue
<AdminTablePagination
  v-model:current-page="pg.currentPage"
  v-model:page-size="pg.pageSize"
  :total="pg.total"
/>
```

- 数据源：`useAdminTablePagination(filteredRows)`（`src/utils/adminTablePagination.ts`）。
- 筛选在前端用 `filterByQuery` + 各筛 ref 组成 `computed` 再传入分页。

可选底栏（虚线弱提示）：

```vue
<p class="{module}-page__hint">…</p>
```

样式对齐 `admin-ruoyi.css` 对 `[class$="-page__hint"]` 的处理。

---

## 6. 逻辑与文件分工

| 职责 | 位置 |
|------|------|
| 子面板 id | `route.meta.stubSecondaryId` 或 `route.meta.billingTab` |
| 假数据 / 类型 | `mock.ts` |
| 列表筛选、持久化、弹层锁 | `*Interactions.ts` |
| 列表 UI | `*Page.vue` 单文件模板分区 |
| 徽章、详情格 | `*.css` |

---

## 7. 新列表页检查清单（PR / 评审用）

- [ ] `section` + `el-card.admin-ep-card` + `__panel` 类
- [ ] `AdminSectionHead` **`toolbar-only`**
- [ ] `AdminListQuery`：左检索筛选重置 / 右主操作
- [ ] 工具栏与操作列按钮间距 **§2.3**（左右 `0.5rem`，无 EP 叠加 `margin-left`）
- [ ] `el-table.admin-ep-table-wrap` + `width: 100%`
- [ ] 数据列仅 `min-width`，无 `align`，无数据列 `width`
- [ ] 操作列 `ADMIN_TABLE_COL_OPS.*` + `fixed="right"` + `admin-ep-row-actions`
- [ ] 单元格 `scope?.row` 守卫
- [ ] `AdminTablePagination` 接在表后
- [ ] 模块 `README.md` 链到本文

---

## 8. 不适用本规范的页面

| 类型 | 示例 | 替代 |
|------|------|------|
| 仪表盘 / 图表 | 工作台、实时大盘 | 分区卡片 + 图表占位，无列表查询栏 |
| 编辑器 | 文档编辑 | 富文本 / 表单页 |
| 权限矩阵 | 角色权限勾选 | 表单 + 树，非 el-table 列表 |
| 纯说明 Tab | 数据范围说明 | 静态文案 |

---

## 10. 模板文件

新建列表页时，推荐复制以下模板文件：

- **模板文档**：[`doc/Admin列表页模板.md`](./Admin列表页模板.md)（含完整 Vue 模板 + CSS 模板 + 规范速查）

---

## 11. 参考文件索引

| 文件 | 说明 |
|------|------|
| `src/views/admin-keys/KeysPage.vue` | **标准实现**（三表） |
| `src/views/admin-keys/keys.css` | 徽章、详情格 |
| `src/views/admin-billing/BillingPage.vue` | 七 Tab 列表（已对齐本规范） |
| `src/components/AdminListQuery.vue` | 查询栏组件 |
| `src/components/AdminSectionHead.vue` | 页眉组件 |
| `src/views/admin-risk/RiskPage.vue` | 已按本规范对齐的风控列表示例 |

---

## 10. 修订

| 日期 | 说明 |
|------|------|
| 2026-05-19 | 首版：从 API 密钥模块沉淀；替代 README 内零散「列表表格」段落为单一真源 |
| 2026-05-19 | **§2.3**：工具栏右侧 `#actions` 与左侧、表格操作列按钮间距统一为 `0.5rem`；全局清零 EP 相邻 margin |
| 2026-05-19 | **§10**：增加「模板文件」章节，引用 `doc/Admin列表页模板.md` |
