# admin-docs · 文档中心（P8）

| 文件 | 说明 |
|------|------|
| `DocsPage.vue` | 四子页：列表 / 编辑与版本 / 发布与回滚 / 可见范围 |
| `mock.ts` | 默认行与 `DOC_PANEL_ORDER`（与 `moduleSecondaryPages` 中 `tai-admin-docs` 一致） |
| `docsInteractions.ts` | `localStorage` 读写 |
| `docs.css` | `doc-page__*` 样式 |

设计依据：**§4.9**；路由前缀 `docs/`、`tai-admin-docs-*`。
