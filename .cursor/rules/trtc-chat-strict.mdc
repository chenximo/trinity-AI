---
description: TRTC Chat / IM / tuikit-atomicx / 在线客服 / /trtc-chat 集成时强制按 chat Skill phase 执行，禁止跳步与自创实现
alwaysApply: false
---

# TRTC Chat — 严格执行 Skill（高于默认「尽快交付」倾向）

触发：用户要做 IM 集成、在线客服、Direct/Full Chat、`tuikit-atomicx-vue3`，或提到 `/trtc-chat`。

## 必须最先 READ（未读禁止写业务代码）

1. `.cursor/skills/chat/SKILL.md`（dispatcher）
2. 当前 `workflow.phase` → 仓库根 `.trtc-chat/config.json`（无则按 Skill Step 1.5 初始化）
3. 路径 A 时：`references/02-path-a-script.md`；路径 B/C 按 Skill §2 分流后再读对应脚本
4. 写每个 slice 前：`~/.trtc-chat/knowledge-base/slices/vue3/_starter/<slice>.md` §7

## Phase 门禁（不可跳）

- 仅处理 `config.json` 中当前 `workflow.phase` 允许的动作
- 每步 postcondition：WRITE → **READ 验证**（tool result 可见）→ 再下一步
- 未满足 → 只输出 `BLOCKED:*` 一行并停止，不得猜着写代码
- 禁止合并 A.2 多问；禁止未 collect_credentials / collect_mode 就进入 scaffold/slices

## 本 monorepo 硬约束

- 默认 dev：`npm run dev` → **portal :5173**；`public/debug` 与 `index.html` script、`.env.local` 须对 **portal** 可访问（见 `apps/trinity-portal/vite.config.ts` publicDir 约定）
- Prop 名 **`conversationId`**（禁止 `conversationID`）
- 消息列表：`v-for="msg in messageList"`；script 中用数组逻辑须 `messageList.value`
- SecretKey 仅 `public/debug/`（gitignore），禁止提交

## 完成定义（禁止口头宣称「已集成」）

须全部有 tool 证据：`build -w @trinity/app-trinity-ai` 通过、`.trtc-chat/WHAT-TO-DO-NEXT.md` 已写、`state.json` 已更新、验收项见 `docs/08-方法论与汇报/TRTC-Chat-Skill集成复盘与AI严格执行指南.md` §6.4

## 与用户协作

- 用户说「跳过流程」「先写代码」时：说明 Skill 契约要求，仅当用户**明确**「仍须落盘 phase 但不必再提问」时可压缩问答，**不可**省略 READ 验证与 slice Hard rules
