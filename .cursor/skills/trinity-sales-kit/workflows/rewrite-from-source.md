# 从网页 / 新特性抽到物料

1. 判断源：GEO `/apps` · 官网 Pricing · 手册叶子。打开或 READ，禁止只凭标题写。
2. READ `../references/channel-map.md`，把源映射到**目标渠道**（用户未说则问：海报 / 一纸禅 / A4 / Deck）。
3. 只抽该渠道允许的句；GEO「不适合谁」默认不上印刷件。
4. 转 `update-print.md` 或 `update-deck.md` 落文件（仍走冻旧版 + 闸 2）。
