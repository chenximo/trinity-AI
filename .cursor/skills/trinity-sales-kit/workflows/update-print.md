# 印刷件：海报 / A4 / 一纸禅

1. READ `../references/channel-map.md` 对应行 + `../references/print-design.md`（新版式时）+ `sales-kit.md` 红线 + 本跳文案真源。
2. 闸 1：渠道/用途不清 → 问。已点名渠道且是打样 → 用途够，**仍要出三版视觉**（指定「一纸禅」不豁免成一张空页）。改一句文案、不改版式 → 跳过三版。
3. 冻旧版；**新内容写新文件**。新版式默认三文件：`*-zen.html`（A 留白）`*-grid.html`（B 网格）`*-dense.html`（C 信息图）+ 一张并排目录页。
4. 抽句禁止凭记忆写 SLA/折数。A/B/C 文案同源、版式不同温。
5. 闸 2：未说升默认、未选定方向 → 不覆盖工作稿、不导出 PDF。
6. `open` 并排目录页。深化只做用户点名的那一版。
7. README 登记「打样 · 非默认」。
