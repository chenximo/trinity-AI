# 更新参数表

## 顺序（硬）

1. 先改 `docs/00-协作与工作流/工程师/API对外接口支持参数.md`
2. 再改 `apps/trinity-docs` 对应 `api/*-parameters.md` 或短页
3. 评估 `trinity-api-acceptance` 用例是否需同步（封发另议）
4. 可选：product `developer-docs.roadmap.yml`

## READ

工程师 md 为目标章节；对外页禁止内部路径。

## 收尾

工程师 review（基本规范 §1.3）+ build。
