{
  "name": "@trinity/pricing-review-worker",
  "version": "0.1.0",
  "private": true,
  "description": "价审出包 Worker：收 Admin Webhook → 跑 pricing CLI → 回写价审包 JSON",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "smoke": "node scripts/smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "tsx": "^4.19.3"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2"
  }
}
