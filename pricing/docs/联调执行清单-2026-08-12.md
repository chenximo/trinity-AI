# 价审 V2 · 联调执行清单（2026-08-12）

> 按顺序勾选；真源：[研发交接-定价价审价目预览-2026-08-12.md](./研发交接-定价价审价目预览-2026-08-12.md)  
> **给研发总包**：[给研发-Backend交付-2026-08-12.md](./给研发-Backend交付-2026-08-12.md)  
> **我方已验项**：[我方调试完成报告-定价运营-2026-08-12.md](./我方调试完成报告-定价运营-2026-08-12.md)

---

## 进度快照（2026-08-12）

| Step | 内容 | 状态 |
|------|------|------|
| 1 | Worker 正式机 sync + V2 smoke | ✅ 已完成 |
| 2 | Admin 出包 URL + 前端发版 | ⏳ 待运维发版 |
| 3 | 正式价审 → 确认 `hy-mt2-plus` | ⏳ 待产品在 Admin 操作 |
| 4 | 转 Backend 文档 | ✅ 文档已齐（见 Step 4） |
| 5 | C-01 联调 | ⏳ 等 Backend |
| — | 商务 CLI L3b/L3a | ✅ 本地已验；Admin 等 S-* |

---

## Step 1 · Worker 正式机同步

**主机**：`43.156.31.5`（ubuntu）· 服务 `trinity-pricing-worker` · 端口 `8787`

### 1.1 本机打 bundle 并同步

```bash
cd trinity-AI
sh pricing/worker/scripts/make-bundle.sh
# 需运维提供密码（勿提交仓库）
export DEPLOY_PASS='***'
python3 pricing/worker/scripts/sync-remote.py
# 期望最后一行：DEPLOY_SYNC_OK
```

或仓根：`npm run pricing:worker:sync`（同上需 `DEPLOY_PASS`）

### 1.2 同步后远程应满足

| 检查 | 命令 / 期望 |
|------|-------------|
| healthz | `curl -sS http://127.0.0.1:8787/healthz` → `dryRun:false` |
| V2 链 | 远程存在 `gen-listing-v2-prices-api.mjs` |
| 触发 smoke | POST trigger → `pricingPolicy: listing_v2_intl_first` |
| catalogSync | 代码含 `catalogSync.ts`；价审成功后日志 `[catalog-sync]` |

### 1.3 `.env` 核对（`/home/ubuntu/trinity-pricing/worker/.env`）

```bash
TRINITY_AI_ROOT=/home/ubuntu/trinity-AI
PRICING_WORKER_DRY_RUN=0
ADMIN_API_BASE=https://<admin-api-host>    # 挂包必配
ADMIN_OPS_TOKEN=<ops-token>                 # 挂包必配
# C-01 后端齐后：
# ADMIN_API_TOKEN=<admin-jwt>
# PRICING_WORKER_CATALOG_SYNC=1
```

---

## Step 2 · Admin 出包 URL + 前端发版

### 2.1 出包 URL（运营后台 · 一次性）

路径：**模型刊例和定价 → 价格校验 → 出包与抓价配置**

| 字段 | 值 |
|------|-----|
| 生文出包 URL | `http://43.156.31.5:8787/v1/pricing-review/trigger` 或内网反代地址 |
| 生图 / 生视频 | 同左（或模板 URL） |

> Admin API 须 **出站** 能访问该 URL。

### 2.2 Admin 前端发版（汇总 UI）

仓库：`TrinityAI-web` · 提交 `3db9ec1`（`feat(admin-pricing): V1/V2 review summary groups`）

```bash
cd TrinityAI-web
git pull origin main
pnpm install
pnpm run build   # 含 apps/admin
# 按现网流程部署 admin 静态资源 / 容器
```

**发版后验收**：打开价格校验 → 确认单有 **将写回 / 维持 V1 / 无草案** 三组小表；Δ% 为 V2 相对 V1。

---

## Step 3 · 正式价审 → 确认写价

### 3.1 触发

1. Admin → **价格校验** → 模态 **生文** → 场景 **官方锚跟刊例**
2. 点 **触发价审**
3. 期望：任务 `ready`，同一 taskId

### 3.2 审单（产品）

| 分组 | 预期 |
|------|------|
| **将写回** | 约 **1 条**：`hy-mt2-plus`（上浮） |
| **维持 V1** | 含 `qwen-max` 等（V2 更低或 fallback） |
| **无草案** | 无官方锚的少数模型 |

**不点确认前**：`GET /v1/prices` 中 hy-mt2-plus 不变。

### 3.3 确认写价

1. 核对仅上浮项 → **确认写价**
2. 抽查 `hy-mt2-plus` 入/出是否变为 V2 建议价
3. 模型 **价格校验** → 已校验

### 3.4 回滚（若误写）

有 **复原** 批次资格时用 Admin 复原；否则走 Backend 运维。

---

## Step 4 · 转 Backend

**发给后台**：[`给研发-Backend交付-2026-08-12.md`](./给研发-Backend交付-2026-08-12.md)（一页；飞书可复制 [`给研发-转述稿.md`](./给研发-转述稿.md)）

Backend 合并后：配 Worker `ADMIN_API_TOKEN` → 再触发价审 → 价目预览应有 xlsx。

---

## Step 5 · C-01 联调（Backend 齐后）

- [ ] 手传 xlsx → GET 列表可见
- [ ] 触发价审 → 数分钟内 catalog 有 `worker-post-review`
- [ ] 价审失败不影响 catalog；catalog 失败不影响价审 ready

---

## 修订

| 日期 | 说明 |
|------|------|
| 2026-08-12 | 初稿：push 后逐步联调清单 |
