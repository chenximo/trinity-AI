# 飞书可复制

---

各位好，定价 **价审主链已有**。本次要交付 **价目预览 + 上游抓价 + 商务价格** 后台 API，请按文档一次做完整：

**`trinity-AI/pricing/docs/给研发-Backend交付-2026-08-12.md`**

**三块能力**  
1. 刊例 Excel 存储（`artifacts` + `catalog/excel` 别名，Admin 已接）  
2. 上游「获取价格」（`POST …/upstream-sources/{code}/fetch`，内嵌 Job）  
3. 商务一键生成（`generate` + `matrix` + `archive` + artifacts 下载）

接口已合并冗余路由（见文档 §1），**不是砍需求**。算法在 trinity-AI Python，Backend **只编排 Job + 存 xlsx**，**勿写** `/v1/prices`，**勿重做**价审。

合并后告知，我们配 Worker token 并联调三条线。

---

*产品/定价 · 2026-08-12*
