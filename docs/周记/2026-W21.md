# 2026-W21 周记

> **周期**：2026-05-18（一）— 2026-05-24（日）

---

## 简版（上交）

### 本周工作

- **文档站一期 MVP**：指南 + API 双轨、OpenRouter 版式、开发分屏编辑；已推 `main`。
- **AI 云**：账户 / 账单 / 发票 P0。
- **运营后台**：登录、OLAP、若依列表、风控 / 大盘 / 密钥 / 计费、工作台与登录 UI。
- **组件库**：登录 / 注册文案与主按钮统一。

### 下周计划

- **文档站**：多模态与 guides 互链、占位充实；产品校验（URL / 异步 / model / 限额）；版式待办收尾。
- **运营后台**：报表 P8、占位扫尾、壳层巡检。
- **用户站**：危险操作确认弹窗等。
- **AI 云**：`home.html` → Vue，门户 `/ai-cloud` 可演示。

### 备注（挑战性 / 卡点）

- 文档产品校验依赖研发 / 产品拍板，未确认前不宜对外发布。
- AI 云首页迁移区块多，建议拆里程碑。
- 后台占位模块需产品排优先级。

---

## 详细版

> 团队存档；**勿复制上交**。

### 本周工作

- **Trinity 文档站（`apps/trinity-docs`）一期 MVP 落地并推远程**
  - 内容：快速入门、管理 API 密钥、模块、多模态（7 子页）、指南（流式 SSE / 请求参数）、参考（错误 / 限额）、FAQ；API Tab（概述 + 对话补全 / 生图 / 生视频）。
  - 版式：对齐 OpenRouter——顶栏 +「文档 | API」二级 Tab、左侧 Tree 导航、右侧 On this page；代码块、表格、提示块样式；侧栏 / 正文 / 目录列间距多轮微调（含宽屏居中导致的空白、目录与正文重叠等问题）。
  - 工程：portal / trinity-ai 开发代理 `/docs`；开发态「编辑本页」分屏（Markdown + `marked` 实时预览，保存成功自动关闭并刷新正式页）。
  - 文档：`Trinity文档站-一期MVP文档清单.md`、OpenRouter 版式对齐规范（执行中）。

- **AI 云用户控制台系统 P0**：账户、账单、发票相关页面（`apps/ai-cloud`）。

- **运营后台**：登录 / 登出、OLAP 一期、若依式列表、风控 / 大盘 / 密钥 / 计费、工作台与登录页 UI 优化。

- **组件库**：登录 / 注册页统一中文文案与 `btn-gradient` 主按钮（`@trinity/ui`）。

### 下周计划

- **文档站**
  - 多模态占位页补文案与示例；`guides` 生图 / 生视频与侧栏多模态页互链。
  - MVP 清单 §6 **产品校验**：生产 `base_url`、生图 / 生视频是否异步及轮询、可用 model 列表、402/429 与错误体字段（需研发 / 产品确认，文档目前多为样例）。
  - 版式收尾：Info / Tip 图标区分等对齐规范里仍待办项（按优先级）。

- **运营后台**：报表中心 P8 实页；对照交付计划扫占位路由；壳层 / 大盘视觉统一巡检。

- **Trinity AI 用户站**：危险操作确认弹窗等（`设计待办` + `ModalPanel`）。

- **AI 云**：`TrinityCloud/home.html` 营销首页迁入 `apps/ai-cloud`，门户 `/ai-cloud` 可演示；补多云 Tab、云能力说明等区块。

### 备注（挑战性 / 卡点）

| 项 | 说明 |
| --- | --- |
| 文档 **产品校验** | 网关真实 URL、异步轮询、model 枚举、计费文案未定时，文档只能写「样例 / 待确认」，容易与实现脱节；**依赖研发接口契约或产品拍板**，否则不宜标「可对外发布」。 |
| **AI 云首页迁移** | `home.html` 区块多（顶栏、四宫格、五云 Tab、能力 / 架构），静态 → Vue 需逐块对照版式规范（15% gutter 等），**工作量偏大**，建议拆 2～3 个可演示里程碑而非一次搬完。 |
| **运营后台占位扫尾** | 模块多、优先级需产品排序；报表 P8 若要做「导出任务」mock，需与列表 / 分页规范一并收口。 |
| 文档站 **build / 发布** | 若下周接 CI 或静态部署，需确认 `base: '/docs/'` 与 portal 反代路径一致（环境差异易踩坑）。 |
