# 2026-W33 周记

> **周期**：2026-08-10（一）— 2026-08-16（日）  
> **周看板**：`week-progress-7.yml` · W33（看板条目待补）  
> **主线**：**定价运营工程化**（V2 价审管道 · Worker · 研发交付包 · 刊例当前/上一版契约）；商务表主路径改拍；上游数据宝洽谈与合同

---

## 简版（上交）

### 本周工作

1. **定价运营工程化（主线）@李玲**  
   承接 W32「文档 + 底座齐、卡在对接」：把刊例价审从「能出建议价」推进到「可触发、可出确认单、可交接研发写价」。  
   - **V2 价审管道**：锁定 V1/V2 双轨与 L-01「只上浮不降」闸；Worker + CLI 本地验过（生文 69 模型；只上浮可写回 **1** 条 `hy-mt2-plus`；其余偏差维持 V1）。正式 Admin **尚未**点「确认写价」，线上 `/v1/prices` 未按 V2 批量调整。  
   - **研发交付包**：08-12 收成一包（价审 / 价目预览存储 / 商务 artifacts）；范围按**正式全量**写，去掉 MVP/P 优先级表述。价目预览 xlsx 能本地生成，**不能**登记到服务器（Backend 缺存储 API）。  
   - **刊例「当前版 / 上一版」**：运营要的是每模态两个槽（确认写价成功才轮转），不是无限历史。前端历史页已接契约；Backend 误推原型已回滚，08-14 另交交付说明请研发按文档重做。  
   - **商务表主路径改拍（08-13）**：真表改「本地脚本生成 → 人审 → Cursor/Admin 上传归档」；Admin 一键 Job 后置。上传 ≠ 写线上刊例。L3a 仍待商务复核后发销售。

2. **上游数据宝 · 商务洽谈与合同 @李玲**  
   与数据宝就上游供应做商务洽谈，处理《AI大模型技术服务协议》（2026.7.8 稿，**08-13 修订**）：服务范围（第三方模型集成 / API Token）、计费与结算方式、权利义务与附件价目。合同字段与价款仍有待填项，**未按已签约写**。

### 下周计划

（对应 W34 · 08-17～08-23）

- **P0**：跟进 Backend 刊例当前/上一版槽；正式环境完成价审确认写价（先 `hy-mt2-plus`）与 Admin 前端发版。  
- **P0**：商务复核 L3a 外发表后发给销售；话术与锁定句对齐。  
- P1：数据宝合同待填项与商务收口（价款 / 结算方式 / 联系人）；签约进度跟进。  
- P1：价目预览 / 商务 artifacts 存储 API 联调（C-01 / 上传归档）。  
- 看板顺延项：Agent SDK / 智能平台 / 支付等按周看板。

### 备注（挑战性 / 卡点）

- 能出 V2 确认单 ≠ 已改线上价；卡点在正式 Admin 发版、出包 URL、人确认写价。  
- 刊例两槽 Backend 未实现（误推已回滚）；前端空列表属预期。  
- L3a 可发 ≠ 生产刊例已切 V2；外发仍等人审。  
- 数据宝协议已改稿，价款与结算方式等仍待双方填齐，签约未闭环。

---

## 详细版

> 团队存档；**勿复制上交**。

### 本周工作

#### A. 定价运营工程化（本周主线）

W32 交付的是体系与 Excel 底座；W33 把「人怎么审、系统怎么接、研发缺哪段 API」写成可执行交接。

| 块 | 本周做了什么 | 关键产物 / 提交 |
| --- | --- | --- |
| **口径锁定** | V1/V2 listing、L3a/L3b 产物、eng backlog 入库 | `c616764`（08-10） |
| **V2 管道 + Worker** | listing V2、review worker、catalogSync、部署清单 | `8bff747` · `58e6d9b`（08-12） |
| **我方已验** | CLI 出包；Worker 正式机 healthz；生文 V2 包 69 模 / 12 diff / 1 可写回 | `我方调试完成报告-定价运营-2026-08-12.md` |
| **研发总包** | 价格校验已通主链；价目预览缺存储 API；交付按全量写 | `研发交接-定价价审价目预览-2026-08-12.md` · `16f6f44` |
| **商务主路径** | 本地生成 + HITL 上传；Admin Job 后置 | `商务价格-本地生成与Cursor人审上传.md` · 折扣总表 SOP（08-13） |
| **刊例两槽** | SOP + 前端契约；Backend `e33b4b9` 已 revert | `刊例当前上一版-Backend交付-2026-08-14.md` · `76723e7` / `1754e52` |

**价审现状（对外说法）**：

```text
触发价审 → 出 V2 vs V1 确认单 ✅（Worker/CLI）
         → 人在正式 Admin 点「确认写价」⏳
         → /v1/prices 才变
```

**Web 侧可见**：价审汇总 V1/V2 分组与写回标签（`3db9ec1`）；历史页当前/上一版 UI（`6d13917`，接口未通时列表为空）。

#### B. 上游数据宝（商务）

| 项 | 说明 |
| --- | --- |
| 角色 | 数据宝作为**上游供应**洽谈对象（乙方：贵州数据宝网络科技有限公司） |
| 合同 | `pricing/【20260813修订】数据宝-2026.7.8-AI大模型技术服务协议.docx`：API Token 计费、先付/后付两种结算、附件模型价目（空表待填） |
| 参考形态 | `pricing/数据宝报价单_2026-07-27.xlsx`（外发表版式参考，W32 已用；本周重心在协议修订） |
| 状态 | 08-13 已出修订稿；联系人/价款/结算勾选仍为占位，**未签约** |

#### C. 提交可见（节选）

| 仓 | 范围 |
| --- | --- |
| trinity-AI | `c616764`～`76723e7`（08-10～08-14） |
| TrinityAI-web | `a62e8b6`～`8644f14`（价审 UI / 刊例槽页 / 同步项） |
| TrinityAI-backend | `54918ae`～`32e646a`（含刊例槽误推与 revert） |

折扣总表脚本、现网线路回灌、Media-Hub 对照等多为工作区未提交。

### 下周计划

| 优先级 | 项 |
| --- | --- |
| P0 | Backend 刊例当前/上一版按 08-14 交付重做；正式 Admin 发版 + 确认写价 |
| P0 | L3a 人工复核 → 销售外发 + 话术 |
| P1 | 数据宝合同待填项收口与签约跟进 |
| P1 | C-01 价目预览存储 / artifacts 上传联调 |
| P2 | 周看板顺延项 |

### 备注（挑战性 / 卡点）

| 项 | 说明 |
| --- | --- |
| **出包 vs 写价** | 确认单已出；写价闸仍要人点；勿说成「已经按 V2 改价」 |
| **两槽** | 产品契约已交；Backend 从零按文档做，勿复用已回滚提交 |
| **商务 Job** | 一键出真表后置；近期以本地 HITL 上传为准 |
| **L3a** | 表在、话术要齐；商务复核仍是外发前提 |
| **数据宝合同** | 修订稿已出；价款/结算/联系人未填齐，勿写已签约 |
