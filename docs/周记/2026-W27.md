# 2026-W27 周记

> **周期**：2026-06-29（一）— 2026-07-05（日）  
> **周看板**：`week-progress-7.yml` · W27 · `result: ⬜`（支付等待外部对接）

---

## 简版（上交）

### 本周工作

1. **官方价目治理（pricing）@李玲**  
   - 搭建 **L1/L3 治理门禁**（`pricing:gate`）、线上刊例拉取与官方种子修复。  
   - **上游比价 pipeline**：刊例对比总表、供应商 vs 官方、Excel 同步（生文 + 生图分模态）。  
   - 接入 **火山方舟、网聚 cloudportal** 等供应商 scrape / normalize；Skill 与 `tools.yaml` 清单、架构文档与 linter。  
   - **生图价目**与生文范式对齐：AIGC 分表 + 汇总、OG-image2 档位映射修复、**≥50% 偏差待核验**机制。  
   - **百炼** Context Cache 分模型比例（Kimi / 万擎 DeepSeek / GLM / MiniMax 等）写入 `cache-policy.mjs` 并重算 `pricing-api.json`。  
   - 告警改为 **钉钉 webhook**（移除 GitHub CI gate 依赖）。

2. **GEO 营销页 @李玲**  
   - 根首页与用户控制台原型 **CTA 串联**；首页与用户控制台视觉对齐。

3. **产品看板 @李玲**  
   - 6 月 W26 结果收口；7 月 W27 计划与模块引用已在看板登记。

### 下周计划

（对齐 W28 · 07-06～07-12）

- **商业化**：支付对接联调、充值 / 计费走查、企业工作区鉴权（`metering-billing`）。  
- **Agent SDK**：L0 架构对齐，P0 `callModel` / `tool` / `stopWhen` API 定稿。  
- **智能平台**：工作台 / Web 孵化器 MVP 原型。  
- **价目**：生文 `pricing:refresh` 全链路验收；百炼 cache 变更纳入供应商对比回归。  
- **文档**：企业全套文档续写；内部工具接入说明（Cursor / Skill 等）。

### 备注（挑战性 / 卡点）

- **支付系统、腾讯云海外模型**仍为跨团队待对接项，W27 计划 ①② 未闭环。  
- **价目对比**依赖多供应商 scrape  freshness；生图仅 **AIGC 国内/国际** 已接入 Excel，TokenHub / 火山为参照。  
- **deepseek-v4-pro** 百炼缓存价须查控制台 explicit 价，不与 input×20% 硬比。

---

## 详细版

> 团队存档；**勿复制上交**。

### 本周工作

| 方向 | 产出 |
| --- | --- |
| **pricing L1/L3** | `validate-official-suppliers`、`pricing:gate`、钉钉告警、`pricing-annotations` |
| **上游 pipeline** | `gen-upstream-pricing` / `gen-upstream-image-pricing`、compare hub、Excel merge |
| **供应商** | volcengine scrape；百炼 cache-policy；wangju cloudportal |
| **Skill** | `trinity-official-pricing` 工作流同步；L2 `tools.yaml` + skill linter |
| **生图** | `channels-image`、`build-image-rows`、trinity-map attribute 过滤、待核验 ≥50% |
| **GEO** | 首页 CTA → 用户控制台（06-29～06-30） |

**可核对提交（存档）**

- `trinity-AI`：`feat(pricing)` 系列（07-02～07-03）；`feat(skills)` tools.yaml（07-02）；`feat(trinity-geo)`（06-29～06-30）。

### 下周计划

见 `week-progress-7.yml` **W28**。

### 备注（挑战性 / 卡点）

| 项 | 说明 |
| --- | --- |
| **支付 / 海外节点** | week-progress-7 blockers，顺延 W28 |
| **价目口径** | 百炼 implicit cache vs 官方 explicit cache hit 需注解区分 |
| **生图刊例** | 线上 Trinity 刊例 vs 官方加价（如 OG-image2 +650%）需 `official-listing` 例外或调价决策 |
