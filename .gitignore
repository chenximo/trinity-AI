# OS
.DS_Store
Thumbs.db

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Logs & local env
*.log
.env
.env.*

# Optional tooling (if you add later)
node_modules/
dist/
.cache/
**/.vitepress/dist
**/.vitepress/cache
**/.vitepress/.temp

# Local image exports (Cursor / design drafts; not repo assets)
generated-images/

# Archives (local backups; avoid bloating the repo)
*.zip

# sync-to-trinityai-web 本地报告与覆盖前备份
scripts/sync-reports/
*.sync-backup-*

# Local tooling scratch (e.g. pip --target installs)
.tmp/

# >>> @tencent-rtc/chat-skills（自动添加，可手动调整）
# 本地配置 + 会话状态（由 dispatcher 在运行时写入）。
# 注意：knowledge-base 已迁移到用户级 ~/.trtc-chat/，不再出现在项目里。
/.trtc-chat/
# <<< @tencent-rtc/chat-skills
